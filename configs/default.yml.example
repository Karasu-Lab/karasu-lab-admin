server:
  port: 3001
  corsOrigin: "https://admin.karasu256.com"

docker:
  socket:
    default: "/var/run/docker.sock"

# Cloudflare Access を使う場合は以下のコメントアウトを外す
# cloudflare:
#   teamDomain: "https://<team>.cloudflareaccess.com"
#   audTag: "<audience-tag>"
